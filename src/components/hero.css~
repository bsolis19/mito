.container {
	display: flex;
	position: relative;
}

.overlay {
	width: 80%;
	text-align: center;
	margin: 0px auto;
	position: absolute;
	left: 0;
	right: 0;
	top: 0;
	bottom: 0;
	display: flex;
	justify-content: center;
	align-items: center;
}

.hero-img {
	position: absolute;
	top: 0;
	left: 0;
	width: 100%;
	z-index: -1;
	height: attr(props.height, 100vh);
}
